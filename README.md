# Talmud Daily — Landing Page

Static landing page for the Talmud Daily program (a project of 18Forty).

Hosted via GitHub Pages → see Settings → Pages.

## Structure

- `index.html` — single-page landing.
- `tdbrand/` — brand assets (wordmark, monogram, pattern SVGs, photos).
